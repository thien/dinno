extends layout

block container
	h1 this is dinno

	if username
		h1 #{username}
		h2 #{password}
	#container
			:markdown-it()
				dinno is a _food sharing_ service running on [node](http://nodejs.org).
	a(href="/register") Register
	p
		| Checkout the files to see how it is organized.
	p
		| Here is an example url
	a(href="/comments/10") Comment with id 10

	include includes/login